apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: roboshop
  labels:
    proj: roboshop
    env: test
    app: nginx
spec:
  type: NodePort
  selector:
    proj: roboshop
    env: test
    app: nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
